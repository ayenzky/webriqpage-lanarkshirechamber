extends layout

block content
  h1.title-header Event Archives
  hr
  .event-wrapper
    - each event in records.events.events
      .event-items
        h2.event-title= event.title
        i(class="fa fa-calendar-plus-o", aria-hidden="true")
          date= dateFormat(event.month +"/"+ event.day +"/"+ event.year, 'dddd, LL')
        br
        .clear
        br
        .event-content
          p!= event.description
